# Processed data

This folder is used for intermediate datasets, model objects, split IDs, and prediction files generated by the analysis scripts.

The processed files are not included in this GitHub repository because they are derived from restricted ELSA Wave 11 data. They can be regenerated locally by running the scripts in order after placing the authorised raw data files in `data/wave11/`.

